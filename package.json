{
  "name": "cli-svg-generator",
  "version": "0.0.1",
  "private": true,
  "description": "Generate an SVG via an interactive command line interface.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json && mv ./build/* .",
    "clean": "rm -rf ./index.js ./lib ./logo.svg",
    "postbuild": "rm -rf ./build",
    "prebuild": "npm run clean"
  },
  "keywords": [
    "svg",
    "command",
    "line",
    "interface",
    "cli",
    "generator"
  ],
  "author": "Snap",
  "license": "ISC",
  "devDependencies": {
    "@types/inquirer": "^9.0.3",
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "inquirer": "^9.2.11",
    "tslib": "^2.6.2",
    "xmlbuilder2": "^3.1.1"
  }
}
