{
  "name": "cli-svg-generator",
  "version": "0.0.1",
  "private": true,
  "description": "Generate an SVG via an interactive command line interface.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json && mv ./build/* . && rm -rf ./build",
    "clean": "rm -rf ./index.js ./lib",
    "prebuild": "npm run clean"
  },
  "keywords": [
    "svg",
    "command",
    "line",
    "interface",
    "cli",
    "generator"
  ],
  "author": "Snap",
  "license": "ISC",
  "devDependencies": {
    "@types/inquirer": "^9.0.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "inquirer": "^9.2.11",
    "tslib": "^2.6.2"
  }
}
